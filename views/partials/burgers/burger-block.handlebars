<li>
	{{burger_name}}

	<button class="change-eat" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}DEVOUR!{{else}}COOK!{{/if}}
	</button>

	<img class="burg" src="/assets/images/burger.png">

</li>
